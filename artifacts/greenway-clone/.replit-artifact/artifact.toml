kind = "web"
previewPath = "/"
title = "Greenway Health"
version = "1.0.0"
id = "artifacts/greenway-clone"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18557

[services.development]
run = "pnpm --filter @workspace/greenway-clone run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/greenway-clone", "run", "build" ]
publicDir = "artifacts/greenway-clone/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18557"
BASE_PATH = "/"
